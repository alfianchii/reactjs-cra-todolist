name: Deploy main to demo

on:
  push:
    branches: ["main"]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🔧
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3

      - name: Deploy 🚀
        run: |
          npm run deploy
